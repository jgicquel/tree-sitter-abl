# Rust artifacts
target/
Cargo.lock

# Node artifacts
build/
node_modules/
package-lock.json

# Swift artifacts
.build/
Package.resolved

# Go artifacts
_obj/

# Python artifacts
.venv/
dist/
*.egg-info
*.whl

# C artifacts
*.a
*.so
*.so.*
*.dylib
*.dll
*.pc
*.exp
*.lib

# Zig artifacts
.zig-cache/
zig-cache/
zig-out/

# Example dirs
/examples/*/

# Grammar volatiles
*.wasm
*.obj
*.o

# Archives
*.tar.gz
*.tgz
*.zip

# Repo docs
docs/*

# Misc
example.p

procedures/
prompts
abl.hash
.codex
ambient

# Local realworld harness config (machine-specific paths)
tools/realworld_files.json

# Local agent config
.claude/

# Generated parser source (built from grammar.js; rebuilt by CI / install)
# Tree-sitter generated parser.c exceeds GitHub's 100 MiB blob limit, so the
# repo regenerates it on demand instead of tracking it.
src/parser.c
